{
  "name": "recroogle",
  "version": "1.0.0",
  "private": true,
  "description": "The only search engine that always finds ME!",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "vite": "^6.0.0"
  }
}
